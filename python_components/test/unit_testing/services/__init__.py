"""
Unit tests for service modules under `services/`.

These are deliberately deterministic and CI-safe:
- No network access
- No external datasets
- Use `tmp_path` for any filesystem interactions
"""


