[pytest]
markers =
    slow: long-running or heavyweight tests (e.g., model downloads); opt-in locally

[pytest]
# Your coursework repo uses `test/` rather than the default `tests/`.
testpaths = test
python_files = test_*.py
addopts = -q


